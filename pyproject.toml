[project]
name = "apache"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.120.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.18.0",
]
